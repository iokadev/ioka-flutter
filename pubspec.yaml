name: ioka
description: Flutter SDK for ioka
version: 0.0.1
homepage: https://ioka.kz
repository: https://github.com/iokadev/ioka-flutter
issue_tracker: https://github.com/iokadev/ioka-flutter/issues

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

platforms:
  android:
  ios:

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.0
  openapi_generator_annotations: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  openapi_generator: ^3.3.0
  build_runner: ^2.1.7

flutter:
